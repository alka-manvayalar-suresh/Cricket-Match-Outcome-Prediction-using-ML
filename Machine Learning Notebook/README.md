# Machine Learning Notebooks

This folder includes all **Jupyter Notebooks** used for model development and experimentation.  
It covers **data preprocessing, feature engineering, model training (Logistic Regression, Random Forest, XGBoost, SVM)**, and **evaluation metrics** such as accuracy, F1-score, and ROC-AUC.
